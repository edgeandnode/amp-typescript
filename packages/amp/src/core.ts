/**
 * Core domain models for the Amp SDK.
 *
 * Includes branded types, schemas, and domain primitives used throughout
 * the SDK — block ranges, dataset references, auth tokens, and more.
 *
 * @module
 */
export * from "./core/domain.ts"
